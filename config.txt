sudo mkdir -p /var/videos/camara1 /var/log/rtsp_grab
sudo chown -R $(whoami):$(whoami) /var/videos /var/log/rtsp_grab
sudo chmod 600 /etc/rtsp-grabber.env



Crea el servicio systemd

Archivo:
/etc/systemd/system/rtsp-grabber.service


Habilita y arranca el servicio

sudo systemctl daemon-reload
sudo systemctl enable rtsp-grabber.service
sudo systemctl start rtsp-grabber.service



Verifica que está corriendo:

sudo journalctl -u rtsp-grabber.service -f


logs en tiempo real

sudo journalctl -u rtsp-grabber.service -f

